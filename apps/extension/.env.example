# Copied to .env for local builds (pnpm build). Vite inlines VITE_* at compile time.
# Production builds: omit or leave empty to use https://api.veritasai.com/api
VITE_API_BASE=http://localhost:4000/api
